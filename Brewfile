# macOS dependencies: `brew bundle` (install.sh runs it for you)

# core
brew "git"
brew "stow"
brew "zsh"
brew "tmux"
brew "neovim"

# cli tools used by the configs
brew "eza"
brew "ripgrep"
brew "fd"
brew "fzf"
brew "zoxide"
brew "bat"
brew "git-delta"

# used from nvim: lazygit via snacks.nvim (<leader>gg)
brew "lazygit"

# needed by Mason (pyright, etc.) in nvim
brew "node"
brew "python"

# font used by alacritty / p10k / eza / neo-tree icons
cask "font-liberation-nerd-font"

# Alacritty is NOT here: its Homebrew cask was disabled on 2026-09-01 because the
# app isn't notarized. install.sh downloads it from GitHub releases instead.
